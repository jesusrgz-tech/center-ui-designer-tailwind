apiVersion: v1
kind: Service
metadata:
  name: tailadmin-react
  labels:
    app.kubernetes.io/name: tailadmin-react
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tailadmin-react
  ports:
    - name: http
      port: 80
      targetPort: 80
